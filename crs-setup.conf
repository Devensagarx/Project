# OWASP CRS setup - minimal defaults
# Paranoia Level defines strictness. Start with 1 to reduce false positives.
SecAction \
 "id:900000,\
  phase:1,\
  nolog,\
  pass,\
  setvar:tx.paranoia_level=1"

# Common exceptions for labs (optional tuning)
# Uncomment to reduce noise:
# SecAction "id:900110,phase:1,nolog,pass,setvar:tx.arg_name_exceptions=username|email"
